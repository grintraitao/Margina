On/off toggle for settings (Herond Shield, privacy options).

```jsx
<Switch checked={on} onChange={setOn} />
<Switch checked={on} onChange={setOn} label="Block trackers" />
```

On-state is Blue Sky. Sizes `sm | md`. Pass `label` to render a clickable row.
