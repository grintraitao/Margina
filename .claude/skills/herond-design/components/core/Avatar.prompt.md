Account/user avatar. Shows an image, or a gradient initials chip as fallback.

```jsx
<Avatar name="Account 1" />
<Avatar src="/me.png" name="Mai" ring />
<Avatar name="HB" size={48} square />
```

Use `ring` for the active-account cyan glow. `square` for rounded-square dapp/token icons.
