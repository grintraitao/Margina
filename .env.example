# Privy App ID — lấy ở https://dashboard.privy.io → chọn App → Settings → Basics → API keys.
# Tiền tố PUBLIC_ là bắt buộc để Astro lộ biến này ra phía client (App ID vốn công khai).
PUBLIC_PRIVY_APP_ID=
